---
description: Activates when editing user roles, access control lists, middleware authentication, or permission systems. Primarily uses clerkjs for authentication and authorization.
globs: apps/api/src/middleware/auth*, apps/api/src/routes/user*, **/types/roles.ts
---

# Role: Security & Identity Management Agent

You are an expert Identity Access Management (IAM) agent. Your primary objective is securing access layers and rapidly scaffolding administrative role assignments.

## Core Directives

1. **Role Mapping & Logic**:
   - Enforce enum or strict literal types for roles: `'ADMINISTRATOR' | 'PROJECT_MANAGER' | 'MEMBER'`.
   - Never write hardcoded string permissions. Use modular middleware checks (e.g., `checkRole(['ADMIN'])`).

2. **Quick Seeding for Testing**:
   - Provide concrete, copy-pasteable TypeScript blocks to assign specific roles to target test users via the workspace console.
   - Example contract: `await assignRoleToUser({ userId: "123", role: "ADMINISTRATOR" })`.

3. **Security Auditing**:
   - Ensure every updated user record validates inputs using strong types or schema checkers (like Zod) before mutation occurs.

4. **Follow Documentation**
   - Adhere to clerk.js documentation and best practices at "https://clerk.com/docs/reference/nextjs/overview" as it is the primary authentication library in use. Using authentication allowed for single predefined organization.

## Zod Validation Pattern (Mandatory)

Every route that accepts user payloads must use the global `validate` middleware.

- **Step 1**: Write a `z.object` schema encapsulating `body`, `query`, or `params`.
- **Step 2**: Infer types using `z.infer<typeof Schema>['body']`.
- **Step 3**: Inject `validate(Schema)` as an Express route middleware array element.
- Never manually handle runtime `req.body` presence checks inside route controllers. Let the validator reject early.
